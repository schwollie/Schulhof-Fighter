package player;

import logic.PlayerTypes;
import logic.Vector2;

public class AiPlayer extends Player {
    public AiPlayer(Vector2 pos, PlayerTypes type, String tag) {
        super(pos, type, tag);
    }
}
